export enum TypeSpeed {
    // TODO: Test this, it probably is in reverse order currently. Higher
    //       number for a higher timeout would indicate this is in reverse.
    SLOW = 100,
    NORMAL = 150,
    FAST = 200
}